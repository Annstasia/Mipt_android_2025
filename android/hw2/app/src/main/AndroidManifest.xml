<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.mipt.annstase.hw2">

    <!--
    Есть warning о том что не стоит блокировать горизонтальное
    положение, но по тз у нас только вертикальное
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Task Planner"
        android:theme="@style/Theme.TaskPlanner"
        android:name=".App"
        >
        <activity
            android:name="ru.mipt.annstase.hw2.ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>